{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${file}"
        },
       {
           "type": "chrome",
           "request": "launch",
           "name": "启动chrome浏览器调试",
           "url": "${file}",
           "sourceMaps": true,
           "webRoot": "${workspaceRoot}"
       }
    ]
}